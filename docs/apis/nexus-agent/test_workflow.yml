info:
  name: test_workflow
  type: http
  seq: 1

http:
  method: POST
  url: 127.0.0.1:8000/chat
  body:
    type: json
    data: |-
      {
        "conversationId": "justanid",
        "message": "Hello nexus"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
